
!.vscode/extensions.json
# ============================================================
# Auto-generated by manage.py on every deploy — do not commit
# Compiled ZIPs are regenerated by Terraform automatically
# CrowdSync | .gitignore
# Downloaded providers — large, always regenerated by tf init
# Local state (real state lives in S3 backend)
# Note: .terraform.lock.hcl IS committed (ensures consistent provider versions)
# Python bytecode
# Saved plan files — binary, machine-specific
# Terraform variable files — may contain secrets
# ── Debug / Scratch Files ────────────────────────────────────
# ── Editor ───────────────────────────────────────────────────
# ── Lambda Build Artifacts ───────────────────────────────────
# ── Terraform ───────────────────────────────────────────────
# ── UI / Node ────────────────────────────────────────────────
# ── macOS ────────────────────────────────────────────────────
**/.DS_Store
*.py[cod]
*.pyo
*.suo
*.sw?
*.tfplan
*.tfvars
*.tfvars.json
.DS_Store
.aws-sam/
.env
.idea/
.terraform/
.vscode/*
__pycache__/
cognito_test.js
dashboard/*.local
dashboard/dist-ssr/
dashboard/dist/
dashboard/node_modules/
dashboard/src/aws-config.ts
debug.js
dist/
get_logs.py
modules/lambda/*.zip
node_modules/
output.json
packaged.yaml
terraform.tfstate
terraform.tfstate.backup
tfplan
